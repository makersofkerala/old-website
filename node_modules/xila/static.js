const fs = require('fs');


let serveStatic = (pathname) => {

    /* . stands for the folder the code is run from. Not this xila folder */
    const staticFolder = path.resolve('./static/');
    if(!fs.existsSync(filePath)) { return notFound };

    try {

	
	const data = fs.readFileSync(filePath);
	return respond(200,data,{"Content-type": getFileType(filePath)});

    } catch(err) {

	console.log("Access restricted to file", filePath);
	return respond(500, "Error accessing the file");

    }

};
